# Client Installer Resources

This directory contains client installer resources that must be committed to git.

Do not place runtime secrets, server TLS private keys, service-side license keys, ClamAV files, build outputs, or machine-local caches in this directory.

When the approved product icon is available, place it here as `icon.ico` and update `electron-builder.yml` to reference it.
